import React from 'react';
import ShowPlaylistsContainer from './containers/ShowPlaylistsContainer';

const ShowPlaylists = (/* store */) => {
  // store.registerReducer();
  // store.runSaga();

  return <ShowPlaylistsContainer />;
};

export default ShowPlaylists;
