import React from 'react';
import Playlists from '.';

describe('Playlist Component', () => {
    it('Should be Playlist Component to be Defined', () => {
        expect(Playlists).toBeDefined();
    });
});
