import { apiFilterChangeReducer } from './apiFilterReducer';
import { combineReducers } from 'redux';

export const Reducers = combineReducers({
  apiFilterChangeReducer
//   otherState: otherReducer
});