import styled from 'styled-components';

export const Form = styled.form`
  padding: 24px;
  height: 100%;
`;

export const EmptyState = styled.div`
  font-size: 14px;
  color: rgba(0, 0, 0, .54);
  margin-top: 24px;
`;

